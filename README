For the cost of an additional build step, you avoid limitations such as not being able to:
- Pass pointers and structs with tag as generic parameters without typedef
- Pass the generic type as a function parameter without typedef
- Implement user-facing interface as actual functions
