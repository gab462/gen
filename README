For the cost of an additional build step, you avoid limitations such as:
- Passing pointers and and struct tags as generic parameters without typedef
- Passing the generic type as a function parameter without typedef
- Implement user-facing interface as actual functions
